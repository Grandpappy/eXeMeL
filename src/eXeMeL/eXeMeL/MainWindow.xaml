<ui:FluentWindow
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  xmlns:views="clr-namespace:eXeMeL.View"
  xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
  xmlns:utilities="clr-namespace:eXeMeL.Utilities"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
  mc:Ignorable="d"
  x:Class="eXeMeL.MainWindow"
  Title="eXeMeL 2"
  Icon="Assets\eXeMeL Icon.ico"
  d:DesignWidth="800" d:DesignHeight="600"
  x:Name="ControlRoot"
  ExtendsContentIntoTitleBar="True"
  MinWidth="884" MinHeight="200"
  >

  <Window.DataContext>
    <Binding Path="Main" Source="{StaticResource Locator}"/>
  </Window.DataContext>

  <Window.InputBindings>
    <KeyBinding Key="F5" Command="{Binding Editor.RefreshCommand}"/>
    <KeyBinding Key="F2" Command="{Binding ToggleEditorModeCommand}"/>
    <KeyBinding Modifiers="Control" Key="F" Command="{Binding FocusOnFindControlCommand, ElementName=ControlRoot}"/>
    <KeyBinding Key="Escape" Command="{Binding ResetFocusCommand, ElementName=ControlRoot}"/>
    <KeyBinding Modifiers="Control" Key="S" Command="{Binding Editor.SaveCommand}"/>
    <KeyBinding Modifiers="Control" Key="O" Command="{Binding Editor.OpenCommand}"/>

    <KeyBinding Modifiers="Alt" Key="D0" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="0" />
    <KeyBinding Modifiers="Alt+Shift" Key="D0" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="0" />
    <KeyBinding Modifiers="Alt" Key="D1" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="1" />
    <KeyBinding Modifiers="Alt+Shift" Key="D1" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="1" />
    <KeyBinding Modifiers="Alt" Key="D2" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="2" />
    <KeyBinding Modifiers="Alt+Shift" Key="D2" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="2" />
    <KeyBinding Modifiers="Alt" Key="D3" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="3" />
    <KeyBinding Modifiers="Alt+Shift" Key="D3" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="3" />
    <KeyBinding Modifiers="Alt" Key="D4" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="4" />
    <KeyBinding Modifiers="Alt+Shift" Key="D4" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="4" />
    <KeyBinding Modifiers="Alt" Key="D5" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="5" />
    <KeyBinding Modifiers="Alt+Shift" Key="D5" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="5" />
    <KeyBinding Modifiers="Alt" Key="D6" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="6" />
    <KeyBinding Modifiers="Alt+Shift" Key="D6" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="6" />
    <KeyBinding Modifiers="Alt" Key="D7" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="7" />
    <KeyBinding Modifiers="Alt+Shift" Key="D7" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="7" />
    <KeyBinding Modifiers="Alt" Key="D8" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="8" />
    <KeyBinding Modifiers="Alt+Shift" Key="D8" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="8" />
    <KeyBinding Modifiers="Alt" Key="D9" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="9" />
    <KeyBinding Modifiers="Alt+Shift" Key="D9" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="9" />
    <KeyBinding Modifiers="Alt" Key="OemMinus" Command="{Binding FoldLevelCommand, ElementName=ControlRoot}" CommandParameter="-" />
    <KeyBinding Modifiers="Alt+Shift" Key="OemMinus" Command="{Binding UnFoldLevelCommand, ElementName=ControlRoot}" CommandParameter="-" />

    <KeyBinding Modifiers="Control" Key="OemPlus" Command="{Binding ZoomInCommand}"/>
    <KeyBinding Modifiers="Control" Key="Add" Command="{Binding ZoomInCommand}"/>
    <KeyBinding Modifiers="Control" Key="OemMinus" Command="{Binding ZoomOutCommand}"/>
    <KeyBinding Modifiers="Control" Key="Subtract" Command="{Binding ZoomOutCommand}"/>
    <KeyBinding Modifiers="Control" Key="D0" Command="{Binding ResetZoomCommand}"/>
    <KeyBinding Modifiers="Control" Key="NumPad0" Command="{Binding ResetZoomCommand}"/>
  </Window.InputBindings>

  <Window.Resources>
    <utilities:AppScaleConverter x:Key="AppScaleConverter"/>

    <!-- AvalonEdit SearchPanel styling for dark themes -->
    <Style TargetType="{x:Type avalonEdit:TextEditor}">
      <Style.Resources>
        <!-- Make SearchPanel readable on dark backgrounds -->
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF2B2B2B"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#FFFFFFFF"/>
      </Style.Resources>
    </Style>

    <!-- Muted icon button for the title bar: no background, opacity-based hover -->
    <Style x:Key="TitleBarIconButton" TargetType="Button">
      <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="8,0"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Opacity" Value="0.7"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="Transparent" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1.0"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.2"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Window chrome button (min/max/close) -->
    <Style x:Key="ChromeButton" TargetType="Button">
      <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Width" Value="46"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="VerticalAlignment" Value="Stretch"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#18FFFFFF"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#28FFFFFF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ChromeCloseButton" BasedOn="{StaticResource ChromeButton}" TargetType="Button">
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#E81123"/>
                <Setter Property="Foreground" Value="White"/>
              </Trigger>
              <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#F1707A"/>
                <Setter Property="Foreground" Value="White"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <!-- Compact flat button for footer zoom controls -->
    <Style x:Key="FooterIconButton" TargetType="Button">
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Padding" Value="5,0"/>
      <Setter Property="FontSize" Value="15"/>
      <Setter Property="Cursor" Value="Hand"/>
      <Setter Property="Opacity" Value="0.7"/>
      <Setter Property="Focusable" Value="False"/>
      <Setter Property="VerticalAlignment" Value="Center"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="Transparent" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Opacity" Value="1.0"/>
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.25"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.LayoutTransform>
      <ScaleTransform ScaleX="{Binding Settings.AppScale}" ScaleY="{Binding Settings.AppScale}"/>
    </Grid.LayoutTransform>
    <Grid.RowDefinitions>
      <RowDefinition Height="46"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Chrome tint overlay — covers entire window but doesn't intercept hits -->
    <Border Grid.RowSpan="2" Background="{DynamicResource ChromeTintOverlayBrush}" IsHitTestVisible="False"/>

    <!-- Custom Title Bar: *, Auto, * — toolbar | find bar | chrome (equal flanking keeps find bar centered) -->
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" MinWidth="280"/>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*" MinWidth="184"/>
      </Grid.ColumnDefinitions>

      <!-- Left: Icon + Title + Tool buttons -->
      <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="12,0,0,0">
        <!-- Vector "XML" icon — accent-colored square with letters cut out -->
        <Path Width="20" Height="20" Margin="0,0,10,0" Stretch="Uniform"
              Fill="{DynamicResource AppAccentBrush}"
              WindowChrome.IsHitTestVisibleInChrome="False"
              Data="F0 M 0,0 L 156,0 156,152 0,152 Z
                    M 22.75,31.22 L 35.76,31.22 46,63.72 57,30.97 68.75,30.97
                    85,96.47 102,30.97 115.25,30.97 116,106.22 137.25,106.72
                    137.25,121.97 104,121.47 104.5,65.72 89.75,121.97 79,121.72
                    62.25,53.97 53,77.47 70.25,121.72 57.25,121.72 45.75,89.22
                    35,121.97 22,121.72 37.75,78.97 Z"/>
        <TextBlock VerticalAlignment="Center" FontSize="13" Margin="0,0,16,0"
                   Foreground="{DynamicResource AppTextBrush}">
          <Run x:Name="AppTitleRun" Text="eXeMeL" FontWeight="SemiBold"/>
          <Run x:Name="AppTitleSuffix" Text=" 2" FontWeight="SemiBold" Foreground="{DynamicResource AppAccentBrush}"/>
        </TextBlock>

        <Button Command="{Binding Editor.RefreshCommand}" ToolTip="Refresh from clipboard (F5)"
                Style="{StaticResource TitleBarIconButton}">
          <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="18"/>
        </Button>
        <Button Command="{Binding Editor.CopyCommand}" ToolTip="Copy all to clipboard"
                Style="{StaticResource TitleBarIconButton}">
          <ui:SymbolIcon Symbol="Copy24" FontSize="18"/>
        </Button>
        <Button Command="{Binding Editor.OpenCommand}" ToolTip="Open file (Ctrl+O)"
                Style="{StaticResource TitleBarIconButton}">
          <ui:SymbolIcon Symbol="FolderOpen24" FontSize="18"/>
        </Button>
        <Button Command="{Binding Editor.SaveCommand}" ToolTip="Save (Ctrl+S)"
                Style="{StaticResource TitleBarIconButton}">
          <ui:SymbolIcon Symbol="Save24" FontSize="18"/>
        </Button>
        <!-- Compact-mode search button — shown only when find bar can't fit in header -->
        <Button x:Name="FindHeaderSearchButton" Click="FindButton_Click" ToolTip="Find (Ctrl+F)"
                Style="{StaticResource TitleBarIconButton}"
                Visibility="Collapsed">
          <ui:SymbolIcon Symbol="Search24" FontSize="18"/>
        </Button>
      </StackPanel>

      <!-- Center: Find bar — collapses to icon when window is too narrow at current zoom -->
      <views:EditorFindView
        x:Name="FindBarInHeader"
        Grid.Column="1"
        DataContext="{Binding Editor}"
        VerticalAlignment="Center"
        WindowChrome.IsHitTestVisibleInChrome="True"/>

      <!-- Right: Settings + chrome buttons -->
      <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Stretch" HorizontalAlignment="Right">
        <Button Click="OpenSettingsButton_Click" ToolTip="Settings"
                Style="{StaticResource TitleBarIconButton}" Margin="0,0,4,0" VerticalAlignment="Center">
          <ui:SymbolIcon Symbol="Settings24" FontSize="18"/>
        </Button>
        <Button Style="{StaticResource ChromeButton}" Click="MinimizeButton_Click">
          <TextBlock Text="&#xE949;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                     Foreground="{DynamicResource AppTextBrush}"/>
        </Button>
        <Button Style="{StaticResource ChromeButton}" Click="MaximizeRestoreButton_Click">
          <TextBlock x:Name="MaximizeRestoreGlyph" Text="&#xE739;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                     Foreground="{DynamicResource AppTextBrush}"/>
        </Button>
        <Button Style="{StaticResource ChromeCloseButton}" Click="CloseButton_Click">
          <TextBlock Text="&#xE106;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                     Foreground="{DynamicResource AppTextBrush}"/>
        </Button>
      </StackPanel>
    </Grid>

    <!-- Main content area -->
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <!-- Tab header row + Snapshots -->
      <Grid Grid.Row="0" Margin="8,4,8,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Tab headers -->
        <StackPanel Grid.Column="0" Orientation="Horizontal">
          <Border x:Name="EditorTabHeader" MouseLeftButtonDown="EditorTabHeader_Click"
                  Cursor="Hand" Padding="10,6" CornerRadius="6,6,0,0"
                  Background="{DynamicResource EditorTintOverlayBrush}"
                  BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                  BorderThickness="1,1,1,0">
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Document24" FontSize="16" Margin="0,0,6,0">
                <ui:SymbolIcon.Style>
                  <Style TargetType="ui:SymbolIcon">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding Editor.IsContentFromFile}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </ui:SymbolIcon.Style>
              </ui:SymbolIcon>
              <ui:SymbolIcon Symbol="Clipboard24" FontSize="16" Margin="0,0,6,0">
                <ui:SymbolIcon.Style>
                  <Style TargetType="ui:SymbolIcon">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding Editor.IsContentFromFile}" Value="False">
                        <Setter Property="Visibility" Value="Visible"/>
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </ui:SymbolIcon.Style>
              </ui:SymbolIcon>
              <TextBlock x:Name="EditorTabLabel" Text="Clipboard Content" VerticalAlignment="Center" FontSize="13"/>
            </StackPanel>
          </Border>

          <Border x:Name="XPathTabHeader" MouseLeftButtonDown="XPathTabHeader_Click"
                  Cursor="Hand" Padding="10,6" CornerRadius="6,6,0,0" Margin="2,0,0,0"
                  Background="Transparent"
                  BorderBrush="Transparent"
                  BorderThickness="1,1,1,0">
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="BranchFork24" FontSize="16" Margin="0,0,6,0" Opacity="0.6"/>
              <TextBlock Text="XPath Tool" VerticalAlignment="Center" FontSize="13" Opacity="0.6"/>
            </StackPanel>
          </Border>

          <Border x:Name="JsonTreeTabHeader" MouseLeftButtonDown="JsonTreeTabHeader_Click"
                  Cursor="Hand" Padding="10,6" CornerRadius="6,6,0,0" Margin="2,0,0,0"
                  Background="Transparent"
                  BorderBrush="Transparent"
                  BorderThickness="1,1,1,0"
                  Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="BranchFork24" FontSize="16" Margin="0,0,6,0" Opacity="0.6"/>
              <TextBlock Text="JSON Tree" VerticalAlignment="Center" FontSize="13" Opacity="0.6"/>
            </StackPanel>
          </Border>

          <Border x:Name="YamlTreeTabHeader" MouseLeftButtonDown="YamlTreeTabHeader_Click"
                  Cursor="Hand" Padding="10,6" CornerRadius="6,6,0,0" Margin="2,0,0,0"
                  Background="Transparent"
                  BorderBrush="Transparent"
                  BorderThickness="1,1,1,0"
                  Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="BranchFork24" FontSize="16" Margin="0,0,6,0" Opacity="0.6"/>
              <TextBlock Text="YAML Tree" VerticalAlignment="Center" FontSize="13" Opacity="0.6"/>
            </StackPanel>
          </Border>

          <Border x:Name="MarkdownPreviewTabHeader" MouseLeftButtonDown="MarkdownPreviewTabHeader_Click"
                  Cursor="Hand" Padding="10,6" CornerRadius="6,6,0,0" Margin="2,0,0,0"
                  Background="Transparent"
                  BorderBrush="Transparent"
                  BorderThickness="1,1,1,0"
                  Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
              <ui:SymbolIcon Symbol="Eye24" FontSize="16" Margin="0,0,6,0" Opacity="0.6"/>
              <TextBlock Text="Preview" VerticalAlignment="Center" FontSize="13" Opacity="0.6"/>
              <Button x:Name="PinPreviewButton" Click="PinPreviewButton_Click"
                      ToolTip="Pin preview side-by-side"
                      Background="Transparent" BorderThickness="0"
                      Padding="4,0" Margin="6,0,0,0" Cursor="Hand"
                      VerticalAlignment="Center" Focusable="False">
                <ui:SymbolIcon x:Name="PinPreviewIcon" Symbol="Pin24" FontSize="14" Opacity="0.6"/>
              </Button>
            </StackPanel>
          </Border>
        </StackPanel>

        <!-- Snapshots inline with tabs (right-aligned) -->
        <views:EditorSnapshotView
          x:Name="EditorSnapshotControl"
          Grid.Column="1"
          DataContext="{Binding Editor}"
          VerticalAlignment="Center"
          HorizontalAlignment="Right"/>
      </Grid>

      <!-- Tab content -->
      <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition x:Name="PreviewSplitterColumn" Width="0"/>
          <ColumnDefinition x:Name="PinnedPreviewColumn" Width="0"/>
          <ColumnDefinition x:Name="SettingsPanelColumn" Width="0"/>
        </Grid.ColumnDefinitions>

        <!-- Editor content (visible by default) -->
        <Border x:Name="EditorPanel" Grid.Column="0" Margin="8,0,8,8"
                Background="{DynamicResource EditorTintOverlayBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1,0,1,1"
                CornerRadius="0,6,6,6"
                Padding="2"
                DataContext="{Binding Editor}">
          <avalonEdit:TextEditor
            x:Name="AvalonEditor"
            FontFamily="{Binding Settings.FontFamily}"
            FontSize="{Binding Settings.EditorFontSize}"
            SyntaxHighlighting="{Binding DataContext.HighlightingManager.HighlightingDefinition, Mode=OneWay, ElementName=ControlRoot}"
            BorderThickness="0"
            Document="{Binding Document}"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
            ShowLineNumbers="{Binding Settings.ShowEditorLineNumbers}"
            WordWrap="{Binding Settings.WrapEditorText}"
            Background="Transparent"
            Foreground="{Binding Settings.EditorBrush}"
            MouseRightButtonDown="AvalonEditor_TextAreaMouseRightButtonDown"
            >
            <avalonEdit:TextEditor.ContextMenu>
              <ContextMenu>
                <MenuItem Header="Copy" Command="Copy"/>
                <MenuItem Header="Cut" Command="Cut"/>
                <MenuItem Header="Paste" Command="Paste"/>
                <Separator/>
                <MenuItem Header="Delve into Decoded XML" Command="{Binding DelveIntoDecodedXmlFromCursorPositionCommand}"/>
                <MenuItem Header="Copy Decoded XML" Command="{Binding CopyDecodedXmlFromCursorPositionCommand}"/>
              </ContextMenu>
            </avalonEdit:TextEditor.ContextMenu>
            <avalonEdit:TextEditor.Options>
              <avalonEdit:TextEditorOptions
                EnableHyperlinks="False"
                EnableEmailHyperlinks="False"
                AllowScrollBelowDocument="True"
                />
            </avalonEdit:TextEditor.Options>
          </avalonEdit:TextEditor>
        </Border>

        <!-- XPath content (hidden by default) -->
        <Border x:Name="XPathPanel" Grid.Column="0" Margin="8,0,8,8"
                Background="{DynamicResource EditorTintOverlayBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1,0,1,1"
                CornerRadius="6,0,6,6"
                Padding="2"
                Visibility="Collapsed">
          <views:XmlUtilityView
            x:Name="XmlUtilityView"
            DataContext="{Binding XmlUtility}"/>
        </Border>

        <!-- JSON Tree content (hidden by default) -->
        <Border x:Name="JsonTreePanel" Grid.Column="0" Margin="8,0,8,8"
                Background="{DynamicResource EditorTintOverlayBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1,0,1,1"
                CornerRadius="6,0,6,6"
                Padding="2"
                Visibility="Collapsed">
          <views:JsonUtilityView
            x:Name="JsonUtilityView"
            DataContext="{Binding JsonUtility}"/>
        </Border>

        <!-- YAML Tree content (hidden by default) -->
        <Border x:Name="YamlTreePanel" Grid.Column="0" Margin="8,0,8,8"
                Background="{DynamicResource EditorTintOverlayBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1,0,1,1"
                CornerRadius="6,0,6,6"
                Padding="2"
                Visibility="Collapsed">
          <views:YamlUtilityView
            x:Name="YamlUtilityView"
            DataContext="{Binding YamlUtility}"/>
        </Border>

        <!-- Markdown Preview content (hidden by default) -->
        <Border x:Name="MarkdownPreviewPanel" Grid.Column="0" Margin="8,0,8,8"
                Background="{DynamicResource EditorTintOverlayBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1,0,1,1"
                CornerRadius="6,0,6,6"
                Padding="2"
                Visibility="Collapsed">
          <views:MarkdownUtilityView
            x:Name="MarkdownUtilityView"
            DataContext="{Binding MarkdownUtility}"/>
        </Border>

        <!-- GridSplitter for pinned preview (hidden by default) -->
        <GridSplitter x:Name="PreviewSplitter" Grid.Column="1"
                      Width="5" HorizontalAlignment="Stretch"
                      Background="{DynamicResource DividerStrokeColorDefaultBrush}"
                      Visibility="Collapsed"
                      Margin="0,0,0,8"/>

        <!-- Settings Panel (slide-in from right) -->
        <Border
          x:Name="SettingsPanel"
          Grid.Column="3"
          Background="{DynamicResource ChromeTintOverlayBrush}"
          BorderBrush="{DynamicResource DividerStrokeColorDefaultBrush}"
          BorderThickness="1,0,0,0">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="16,16,16,8">
              <TextBlock Text="Settings" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
              <ui:Button HorizontalAlignment="Right" VerticalAlignment="Center"
                         Appearance="Transparent"
                         Icon="{ui:SymbolIcon Dismiss24}"
                         Click="CloseSettingsButton_Click"
                         Padding="6"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
              <StackPanel Orientation="Vertical" Margin="0,0,0,16">
                <views:SettingsView DataContext="{Binding Settings}"/>
                <ui:Button x:Name="ChangeLogButton" Content="View Change Log"
                           Click="ChangeLogButton_Click" Margin="16,8"
                           HorizontalAlignment="Left" Appearance="Transparent"/>
              </StackPanel>
            </ScrollViewer>
          </Grid>
        </Border>
      </Grid>

      <!-- Update toast notification -->
      <Border x:Name="UpdateToast" Grid.Row="2" Visibility="Collapsed"
              Background="{DynamicResource AppAccentBrush}" Padding="12,8"
              CornerRadius="0">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>
          <TextBlock x:Name="UpdateToastMessage" Grid.Column="0"
                     Text="A new version is available!"
                     VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold"
                     Foreground="#FF1E1E1E"/>
          <Button Grid.Column="1" Content="Update now and restart eXeMeL"
                  Click="UpdateNowButton_Click" Margin="8,0"
                  Padding="10,4" FontSize="12" Cursor="Hand"/>
          <Button Grid.Column="2" Content="Update later"
                  Click="UpdateLaterButton_Click" Margin="0,0,8,0"
                  Padding="10,4" FontSize="12" Cursor="Hand"/>
          <Button Grid.Column="3" Content="&#xE10A;" FontFamily="Segoe MDL2 Assets"
                  Click="UpdateDismissButton_Click"
                  Background="Transparent" BorderThickness="0"
                  Padding="4" FontSize="10" Cursor="Hand"
                  Foreground="#FF1E1E1E" VerticalAlignment="Center"/>
        </Grid>
      </Border>

      <!-- Footer / Status Bar with gold accent -->
      <Border Grid.Row="3" Padding="12,6"
              BorderThickness="0,1,0,0" BorderBrush="{DynamicResource AppAccentBrush}">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="92"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="{Binding Status}"
                     TextTrimming="CharacterEllipsis" ToolTip="{Binding Status}"
                     FontSize="13" Opacity="0.7" VerticalAlignment="Center"/>
          <TextBlock Grid.Column="1" Text="{Binding ToolInformation}"
                     TextTrimming="CharacterEllipsis" FontSize="13" Opacity="0.7"
                     HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
          <!-- Zoom controls -->
          <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
            <Button Command="{Binding ZoomOutCommand}" ToolTip="Zoom out (Ctrl+-)"
                    Style="{StaticResource FooterIconButton}">&#x2212;</Button>
            <Button x:Name="ZoomLevelButton"
                    Content="{Binding Settings.AppScale, Converter={StaticResource AppScaleConverter}}"
                    Click="ZoomLevelButton_Click"
                    Style="{StaticResource FooterIconButton}"
                    MinWidth="44"
                    ToolTip="Select zoom level"/>
            <Button Command="{Binding ZoomInCommand}" ToolTip="Zoom in (Ctrl++)"
                    Style="{StaticResource FooterIconButton}">+</Button>
          </StackPanel>
          <!-- Content type toggle — fixed-width column so zoom controls don't shift -->
          <TextBlock x:Name="ContentTypeLabel" Grid.Column="3"
                     Text="XML" FontSize="13" FontWeight="SemiBold"
                     TextAlignment="Center"
                     Foreground="{DynamicResource AppAccentBrush}" Cursor="Hand" VerticalAlignment="Center"
                     ToolTip="Click to toggle between XML and JSON mode"
                     MouseLeftButtonDown="ContentTypeLabel_Click"/>
        </Grid>
      </Border>
    </Grid>

    <!-- Find overlay — compact-mode only, positioned under the search button via code-behind -->
    <Border x:Name="FindOverlayPanel"
            Grid.RowSpan="2"
            HorizontalAlignment="Left" VerticalAlignment="Top"
            Margin="0,50,0,0"
            Visibility="Collapsed"
            Panel.ZIndex="200"
            Background="{DynamicResource OverlayBackgroundBrush}"
            BorderBrush="{DynamicResource BlackBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="8,6">
      <Border.Effect>
        <DropShadowEffect BlurRadius="12" ShadowDepth="3" Direction="270" Opacity="0.5" Color="Black"/>
      </Border.Effect>
      <views:EditorFindView
        x:Name="FindBarInOverlay"
        DataContext="{Binding Editor}"/>
    </Border>
  </Grid>
</ui:FluentWindow>
